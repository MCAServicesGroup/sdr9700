# Local activity files

Use this directory for ordinary local task files such as downloads, reports,
exports, scratch notes, and task-specific scripts. Only this guide is tracked;
the other contents of `Local/` are ignored and are not restored by cloning the
repository.

Do not store credentials or sensitive operational data here. Put those files
under the workspace's private-data location described in
`/mnt/workspace/AGENTS.md`.
